CREATE TABLE tbl_users (

    -- ##################
    -- Column definitions
    -- ##################

    user_id VARCHAR(512) NOT NULL,
    user_password VARCHAR(512) NOT NULL,
    PRIMARY KEY (user_id)
);